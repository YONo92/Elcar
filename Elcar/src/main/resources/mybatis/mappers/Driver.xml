<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
      PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.elcar.driver.DriverDAO">

	<!-- 드라이버 신청 등록하기 -->
	<insert id="insertDriver" parameterType="driver">
		<![CDATA[
			INSERT INTO driver(num, id, name, img, getdate, carnum, cartype) VALUES 
			(#{num}, #{id}, #{name}, #{img}, #{getdate}, #{carnum}, #{cartype});
		]]>
	</insert>



</mapper>